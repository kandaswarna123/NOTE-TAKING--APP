import React from 'react';
import NoteForm from '../components/NoteForm';

const AddNotePage = () => {
  return (
    <div>
      <h1>Add Note Page</h1>
      <NoteForm />
    </div>
  );
};

export default AddNotePage;
